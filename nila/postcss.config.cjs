/**
 * Nila styles are plain CSS — no PostCSS transforms are needed. This empty
 * config exists solely to stop Vite from walking up the directory tree and
 * loading the parent project's Tailwind-based PostCSS config.
 */
module.exports = { plugins: {} }
